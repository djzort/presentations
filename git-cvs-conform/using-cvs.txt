Your config file

$ cat ~/.cvsrc
update -Pd
diff -u
#checkout -P

$ cat ~/.cvspass
# # # # # # # # # # #


Add a file

$ cvs add whatever.file

Remove a file

$ rm foobar.file
$ cvs rm foobar.file

Commit (all) changes, prompting for message

$ cvs commit

Commit (all) changes, cli message

$ cvs commit -m 'commit comment'

Creating a tag

$ cvs tag release_20121212

Creating a tag (annotated)

$ echo 'No such function in cvs'

Checkout a tag

$ cvs checkout -r release_20121212 my_module

Delete a tag

$ cvs rtag -d release_20121212 my_module