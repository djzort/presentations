Your config file

$ cat ~/.gitconfig
[user]
	name = Dean Hamstead
	email = dean@optusnet.com.au
[color]
	ui = auto
[push]
	default = simple

Add a file

$ git add whatever.file

Remove a file

$ git rm foobar.file


Commit (all) changes, prompting for message

$ git commit -a

Commit (all) changes, cli message

$ git commit -a -m 'commit comment'

Creating a tag

$ git tag release_20121212

Creating a tag (annotated)

$ git tag -a release_20121212 -m 'My comment'

Checkout a tag

$ git checkout -b newlocalbranch release_20121212

Delete a tag

$ git tag -d release_20121212